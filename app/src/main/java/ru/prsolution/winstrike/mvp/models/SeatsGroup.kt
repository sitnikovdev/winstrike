package ru.prsolution.winstrike.mvp.models

class SeatsGroup(json: Array<String>) {
    var dx: Double = 0.0
    var dy: Double = 0.0
    var angle: Double = 0.0
    var title: String? = null
    var seats = arrayListOf<Seat>()

    init {
/*          let x = json["x"] as? Double,
            let y = json["y"] as? Double,
            let angle = json["angle"] as? Double,
            let seats = json["seats"] as? [Any]

        this.dx = x
        this.dy = y
        this.angle = angle
//        this.title = json["title"] as? String
        for (seatData in seats) {
//                let seatJson = seatData as? [String: Any],
            var seat = Seat(arrayOf("1", "2", "3"), "", "", "", false)
            this.seats.add(seat)*/
        }
    }
